# DECISIONS

## 1. החלטות ארכיטקטוניות

- ה־Controller מטפל רק ב־HTTP, ה־Service מרכז את הלוגיקה העסקית והטרנזקציות, וה־Repository אחראי לגישה למסד הנתונים. בחרתי בהפרדה פשוטה ללא שכבות מיותרות.
- בצד ה־frontend השתמשתי ב־Reactive Forms של Angular, משום שהן מאפשרות validation ברור ובדיקות פשוטות.
- הוספתי validation לשדות החובה ולטווח התאריכים, כך שתאריך הסיום לא יכול להיות לפני תאריך ההתחלה.
- מספר הימים מחושב מהתאריכים ולא מוזן ידנית, ולכן לא ניתן ליצור מספר ימים שלילי.
- לאחר יצירת בקשה מוצגת הודעת הצלחה, ובמקרה של כשל מוצגת הודעת שגיאה ברורה, ללא `alert`.
- בפעולת האישור הוספתי מצב טעינה נפרד לכל בקשה כדי למנוע לחיצות כפולות.
- לאחר אישור מוצלח רק הרשומה הרלוונטית מתעדכנת, ללא טעינה מחדש של כל הרשימה.
- שגיאות מוצגות ליד הבקשה בשפה קצרה וברורה למשתמש, ללא `alert`.
- הוספתי `LeaveRequestsService` ייעודי לכל קריאות ה־HTTP והגדרתי enums ו־DTOs במקום `any`. טעינת הנתונים הראשונית מאוחדת עם `forkJoin`; בקשות HTTP מסתיימות אוטומטית ולכן אין subscriptions ארוכי־חיים או צורך ב־state management נוסף.
- הוספתי Bean Validation ל־DTO של יצירת בקשה, כולל שדות חובה ובדיקת טווח תאריכים, כדי להגן על ה־API גם כאשר לא משתמשים ב־frontend.

## 2. הבאג ביתרת החופשה

- הבאג: מספר הימים שכבר נוצלו (`used`) חושב, אך לא נכלל בבדיקת המכסה.
- התיקון: שיניתי את התנאי ל־`used + days > annualQuota`.
- הטסט: הוספתי טסט לעובד שניצל 18 מתוך 20 ימים ומבקש 3 ימים נוספים. הבקשה נדחית עם שגיאת 400.
- בנוסף גיליתי שהמערכת חישבה ימי חופשה מאושרים מכל השנים יחד. שיניתי את החישוב כך שיכלול רק בקשות מהשנה של הבקשה הנוכחית, והוספתי טסט שמוודא שחופשה משנת 2025 אינה מפחיתה את המכסה של 2026.

## 3. אישור בקשה (approve) ו-concurrency

- בקשה שלא קיימת מחזירה 404, ובקשה שכבר טופלה מחזירה 409.
- האישור מתבצע בטרנזקציה עם נעילה פסימית על הבקשה והעובד. לאחר הנעילה היתרה נבדקת שוב, ולכן אישורים מקבילים לא יכולים יחד לחרוג מהמכסה.
- הוספתי טסט מקבילי מול PostgreSQL אמיתי: שתי בקשות מאושרות בו־זמנית, ורק אחת מהן מצליחה כאשר יחד הן חורגות מהמכסה.
- חיזקתי את הטסט כך שיוודא שבסיום אושרו בדיוק 20 ימי חופשה.

## 4. מה לא בוצע בגלל מגבלת זמן

- לא הוספתי authentication ו־authorization.
- לא הוספתי migrations באמצעות Flyway או Liquibase.
- ה־API עדיין מחזיר entities במקום response DTOs.
- לא שדרגתי את Angular לגרסה חדשה, משום ששדרוג כזה דורש בדיקות regression נפרדות.

## 5. שימוש ב-AI

### דוגמאות לשימוש ב-AI

1. Prompt: “Do deep analysis and explain the vacation balance bug.”
   - ה־AI עזר לעקוב אחרי החישוב וגילה שהימים שכבר נוצלו לא נכללו בבדיקת המכסה.
2. Prompt: “How should we implement the approve endpoint and handle concurrent approvals?”
   - ה־AI הציע טרנזקציה ונעילה פסימית על הבקשה והעובד, כדי למנוע חריגה מהמכסה באישורים מקבילים.
3. Prompt: “Review the controller, service and repository responsibilities.”
   - ה־AI עזר להעביר את הלוגיקה העסקית ל־Service ולהשאיר את ה־Controller אחראי רק על HTTP.

### איפה דחיתי/תיקנתי הצעה של AI

- ה־AI הציע להציג הודעת שגיאה כללית: `Could not approve request`. דחיתי את ההצעה כי ההודעה אינה מסבירה למשתמש מה קרה. במקום זאת הוספתי הודעות קצרות וברורות לפי קוד ה־HTTP, למשל `Not enough vacation days` ו־`Request already processed`.

### אבטחה

- נמצא SQL injection בחיפוש לפי שם עובד, בגלל שרשור הקלט לתוך שאילתת SQL. החלפתי את השאילתה במתודת Spring Data עם פרמטרים.

### שיפורים נוספים

- הוספתי טיפול בשגיאה בטעינה הראשונית, כדי שלא תוצג טבלה ריקה ללא הסבר.
- התאריכים מוצגים באמצעות `DatePipe` עם timezone של `UTC`, כדי למנוע שינוי של יום בגלל אזור הזמן.
- הוספתי טסט שמוודא שלא ניתן לאשר בקשה שנדחתה ושה־API מחזיר 409.
